import { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = {
  appId: "mx.yavoy.usuario",
  appName: "Ya Voy",
  webDir: "dist",
  server: { androidScheme: "https" }
};
export default config;
